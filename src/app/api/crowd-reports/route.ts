import { NextResponse } from 'next/server';

export async function POST() {
  // Placeholder — will validate and store crowd reports
  return NextResponse.json({
    message: 'Crowd reports API — coming soon',
  });
}
