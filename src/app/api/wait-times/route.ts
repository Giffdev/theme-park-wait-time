import { NextResponse } from 'next/server';

export async function GET() {
  // Placeholder — will be replaced with Firestore reads
  return NextResponse.json({
    message: 'Wait times API — coming soon',
    data: [],
  });
}
